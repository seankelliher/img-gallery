Image gallery where users click on a thumbnail to view the image in a larger size and with a caption.

The gallery has minimal styling and a few media queries to scale the fonts. You would probably want to add more of both for a production model. All functionality is accomplished through "vanilla JavaScript."
